# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Local environment
.env
.env.local
.env.*.local

# Server JSON-file store (dev fallback backend — not source)
server/*-db.json
server/*-db.json.tmp
server/*.tmp
server/*.corrupt.*

# Tool caches
.eslintcache

# Vendored agent skills (reinstall with: npx skills add — see skills-lock.json)
.agents/
.claude/skills/

# Catalyst CLI project link (contains project/org ids, machine-specific)
.catalystrc

# Scratch dir used by `pnpm run lock:npm`
.lockgen/
catalyst-debug.log
