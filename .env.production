# Production build profile for the same-origin Spring Boot service.
#
# Leave this empty when the built frontend is served by the Java backend. Set a
# full URL only for an intentionally split-origin deployment.
VITE_API_BASE_URL=
