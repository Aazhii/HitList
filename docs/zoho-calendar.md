# Zoho Calendar

Zoho Calendar connect and import are unavailable in the PostgreSQL-only Java
migration. There is no Zoho Calendar API route, OAuth callback, credential
configuration, or UI workflow. The app's Calendar is limited to HitList tasks
and database records.
